-- ============================================
-- 用户种子数据（SQL 版本）
--
-- 注意：密码哈希由 seed.py 动态生成。
-- 推荐使用 seed.py 而非直接执行此 SQL：
--     python database/mysql/seed.py
--
-- 如果必须使用纯 SQL，请先生成 bcrypt 哈希后替换下方的占位符。
-- ============================================

USE eim_agent_qa;

-- 管理员账号: admin / admin123
-- 普通用户账号: user / user123
-- 密码字段存储的是 bcrypt 哈希，实际值由 seed.py 生成。
-- 此处仅作为参考模板，请执行 seed.py 完成数据初始化。
